@model WebMessenger.Models.Request[]

<label class="General_MainFrameHeading">Open Requests</label>

@{
    if (Model != null) {
        if (Model.Length > 0) {
            foreach (Request req in Model) {
                <label>@req.Sender.Name is sending a chat request</label>
            }
        } else {
            <label>No open Requests</label>
        }
    }
}
